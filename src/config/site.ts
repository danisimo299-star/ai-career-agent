export const siteConfig = {
  name: "ProfyMind",
  // English fallback only — the locale-aware copy search engines and social
  // previews actually see comes from `dict.seo` (see `src/app/layout.tsx`'s
  // `generateMetadata`), not this field.
  description:
    "ProfyMind is a personal AI career assistant. Find the right profession, assess your skills, build a career plan and resume, prepare for interviews, and discover matching jobs.",
  // The canonical production host — see `next.config.ts`'s `www` redirect
  // for why this must never be the `www` subdomain or a preview URL.
  url: "https://profymind.ru",
};
